# Reproducible build of the thesis DOCX from the Markdown sources.
#
# Install inside the project virtual environment:
#   .venv/bin/python -m pip install -r requirements/docs.txt
# Build by supplying the official template explicitly:
#   .venv/bin/python scripts/build_thesis_docx.py --template /path/to/template.docx

python-docx==1.2.0
python-pptx==1.0.2
docxcompose==2.2.0
pypandoc_binary==1.17
lxml==6.1.1
